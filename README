#Conteudo de login pode servir de exemplo

    <?php if ($data) : ?>
        <h1>Tem dados</h1>
        <?php foreach ($data as $login) : ?>
            <p><?= $login["nm_login"]; ?></p>
        <?php endforeach; ?>
    <?php else : ?>
        <h1>Não tem dados</h1>
    <?php endif; ?>